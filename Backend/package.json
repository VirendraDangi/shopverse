{
  "name": "shopverse",
  "version": "1.0.0",
  "description": "Fake product API using json-server",
 "scripts": {
"start": "json-server --watch db.json --port $PORT --static ./public"

},
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
